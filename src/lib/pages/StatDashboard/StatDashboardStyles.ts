import Row from "antd/es/row";
import styled from "styled-components";

const DayDashboardStyles = styled.div``;

export const StyledRow = styled(Row)`
  padding: 10px 15px;
`;

export default DayDashboardStyles;
