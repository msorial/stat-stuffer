import Row from "antd/es/row";
import styled from "styled-components";

const PrizePicksDashboardStyles = styled.div``;

export const StyledRow = styled(Row)`
  padding: 10px 15px;
`;

export default PrizePicksDashboardStyles;
